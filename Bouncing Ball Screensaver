import pygame
import random
pygame.init()
window = pygame.display.set_mode([1018, 573])
window.fill((0,0,0))
drawing = True
while drawing:

    for event in pygame.event.get():
        if event.type == pygame.QUIT:
            drawing = False

